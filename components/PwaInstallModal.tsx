import React from 'react';

// This component is no longer used since DEMO mode and its associated PWA features have been removed.
const PwaInstallModal: React.FC = () => null;

export default React.memo(PwaInstallModal);